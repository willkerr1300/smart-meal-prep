version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: meal_prep_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mealprep
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d mealprep"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
